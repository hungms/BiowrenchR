% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-strips.R
\name{add_strip_pal}
\alias{add_strip_pal}
\title{Recolor facet strip backgrounds}
\usage{
add_strip_pal(plot, pal)
}
\arguments{
\item{plot}{A ggplot object with facet strips.}

\item{pal}{Character vector of colors (e.g. from \code{\link[=get_palette]{get_palette()}}).}
}
\value{
A ggplotified gtable (requires \strong{ggplotify}).
}
\description{
Sets strip rectangle fills for a faceted ggplot using colors from \code{pal}
(recycled if there are more strips than colors).
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  facet_wrap(~cyl)
add_strip_pal(p, get_palette("pal_kelly20", n = 3))
}

}
