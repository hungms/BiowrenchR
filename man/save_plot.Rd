% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-export.R
\name{save_plot}
\alias{save_plot}
\title{Save a plot to PDF}
\usage{
save_plot(
  plot,
  save_dir = ".",
  file = NULL,
  width = getOption("repr.plot.width", 7),
  height = getOption("repr.plot.height", 7)
)
}
\arguments{
\item{plot}{A plot object printable with \code{print()} (e.g. ggplot).}

\item{save_dir}{Output directory (created if missing).}

\item{file}{Optional file name or path. If a bare name without directory,
it is placed under \code{save_dir}. If \code{NULL}, uses the unevaluated \code{plot}
argument name (only reliable when calling \code{save_plot()} directly).}

\item{width}{Plot width in inches. Defaults to \code{getOption("repr.plot.width", 7)}.}

\item{height}{Plot height in inches. Defaults to \code{getOption("repr.plot.height", 7)}.}
}
\value{
The output file path, invisibly.
}
\description{
Writes \code{plot} as a PDF. Uses Cairo when available.
}
\examples{
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
save_plot(p, tempdir(), file = "mtcars_scatter.pdf")
}

}
