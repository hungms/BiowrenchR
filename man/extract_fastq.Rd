% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crick.R
\name{extract_fastq}
\alias{extract_fastq}
\title{Build a paired-FASTQ sample table from an SRA download directory}
\usage{
extract_fastq(
  dir,
  sample,
  run_id,
  replicate,
  fastq1_pattern = "1.fastq.gz$",
  fastq2_pattern = "2.fastq.gz$",
  format
)
}
\arguments{
\item{dir}{Directory containing FASTQ files.}

\item{sample}{Sample name(s) to assign.}

\item{run_id}{Run / accession ID prefix(es) used to filter files
(matched against the substring before the first \verb{_}).}

\item{replicate}{Replicate ID(s) to assign.}

\item{fastq1_pattern}{Regex for read-1 files. Default \code{"1.fastq.gz$"}.}

\item{fastq2_pattern}{Regex for read-2 files. Default \code{"2.fastq.gz$"}.}

\item{format}{Either \code{"atac"} or \code{"rna"} (currently used for validation only).}
}
\value{
A data frame with columns \code{sample}, \code{fastq_1}, \code{fastq_2}, and
\code{replicate}.
}
\description{
Filters FASTQ files whose name prefix matches \code{run_id}, pairs read 1 / read 2
by pattern, and returns a samplesheet-like data frame for ATAC or RNA
pipelines.
}
\examples{
\dontrun{
extract_fastq(
  dir = "sra_fastqs",
  sample = "sampleA",
  run_id = "SRR123",
  replicate = 1,
  format = "rna"
)
}

}
